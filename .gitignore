# Dependencies
node_modules
.pnpm-store

# Next.js
.next
out

# Environment variables — NEVER commit these
.env*
!.env.example

# Credential files — NEVER commit these
client_secret*.json
*.credentials.json
service-account*.json

# Database
*.db
*.db-shm
*.db-wal

# npm pack artifacts
*.tgz

# OS
.DS_Store
Thumbs.db

# Editor
.vscode
.idea
*.swp

# Build cache
tsconfig.tsbuildinfo

# Bench artifacts (runtime output, regenerated by `node scripts/bench-skill-runtime.mjs`)
scripts/.bench-skill-runtime.last.json

# Project instructions (private)
CLAUDE.md

# Internal client-specific automation (NEVER commit — contains client GTM assets)
scripts/hedi/
TEAM_SETUP.md

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
.vercel

# Local development artifacts
.reply-tracking/
gtmos-day30-review.html
templates/luma-onboarding.json

# Lead data and intelligence (may contain sensitive info)
data/leads/*
data/intelligence/*
data/campaigns/*
data/content/*
data/scrapes/
!data/leads/.gitkeep
!data/intelligence/.gitkeep
!data/intelligence/README.md
!data/campaigns/.gitkeep
!data/content/.gitkeep
gtm-os.db.bak

# Local-only runtime / per-tenant context (lives at ~/.gtm-os/, never commit)
/gtm-os.yaml
/clients/
.herenow/

# Per-campaign gold fixtures (real lead data)
gold-fixtures/

# Campaign-specific scratch scripts (local working files, not framework code)
scripts/c8-*
scripts/c9-*
scripts/claude-code-attendee-*
scripts/seo-hire-*

# generated changesets (NEVER commit)
CHANGES.md